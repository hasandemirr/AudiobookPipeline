# Pure-PyPI service deps. torch/torchaudio NOT here (cu121 index, setup_python.ps1).
# numpy handled in setup_python.ps1 + pinned transitively by chatterbox.
# nvidia-ml-py (not pynvml): provides the `pynvml` module WITHOUT the deprecation
# warning that aborts setup_python.ps1's CUDA check under ErrorActionPreference=Stop.
fastapi
uvicorn[standard]
nvidia-ml-py
